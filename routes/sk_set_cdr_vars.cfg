route[SET_CDR_VARS]{
    xlog("L_INFO", "CDR HERE $fU -- $si -- $fd -- $tU -- $ci \n");
    $dlg_var(caller) = $fU;
    $dlg_var(caller_ip) = $si ;
    $dlg_var(caller_domain) = $fd;
    $dlg_var(callee) = $tU;
    $dlg_var(call_id) = $ci;
}
